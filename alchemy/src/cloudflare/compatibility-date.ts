import { supportedCompatibilityDate } from "miniflare";

/**
 * The default Cloudflare Workers compatibility date, set based on the current workerd release used by Miniflare.
 */
export const DEFAULT_COMPATIBILITY_DATE = supportedCompatibilityDate;
